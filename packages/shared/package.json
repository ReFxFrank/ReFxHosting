{
  "name": "@refx/shared",
  "version": "0.1.0",
  "description": "Shared TypeScript contract (enums, DTOs, panel↔agent protocol, permissions) for ReFx Hosting.",
  "license": "AGPL-3.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(no eslint config for shared)\" && exit 0",
    "test": "echo \"(no tests)\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
