{
  "name": "Rust",
  "slug": "rust",
  "author": "ops@refx.example",
  "description": "Facepunch's survival sandbox. SteamCMD-installed dedicated server with Oxide/uMod support and live map configuration.",
  "category": "survival",
  "deployMethods": [
    "DOCKER",
    "NATIVE_PROCESS"
  ],
  "supportsLinux": true,
  "supportsWindows": false,
  "dockerImages": {
    "Default": "ghcr.io/parkervcp/steamcmd:debian",
    "uMod": "ghcr.io/parkervcp/steamcmd:debian"
  },
  "steamAppId": 258550,
  "startupCommand": "bash refx-rust.sh",
  "startupDetect": "Server startup complete",
  "stopCommand": "quit",
  "installScript": {
    "container": "ghcr.io/parkervcp/steamcmd:debian",
    "entrypoint": "bash",
    "script": "#!/bin/bash\n# ReFx steamcmd shim \u2014 the steamcmd:debian/proton install image ships no\n# `steamcmd` on PATH. Bootstrap SteamCMD and expose it on PATH so the egg's\n# `steamcmd ...` calls resolve (otherwise the game server never downloads).\nif ! command -v steamcmd >/dev/null 2>&1; then\n  mkdir -p /mnt/server/steamcmd\n  curl -sSL https://steamcdn-a.akamaihd.net/client/installer/steamcmd_linux.tar.gz | tar zxf - -C /mnt/server/steamcmd || { echo \"ERROR: failed to fetch steamcmd\"; exit 1; }\n  chmod +x /mnt/server/steamcmd/steamcmd.sh /mnt/server/steamcmd/linux32/steamcmd 2>/dev/null || true\n  printf '#!/bin/sh\\nexec /mnt/server/steamcmd/steamcmd.sh \"$@\"\\n' > /usr/local/bin/steamcmd\n  chmod +x /usr/local/bin/steamcmd\nfi\nset -euo pipefail\n# --- ReFx SteamCMD update safety (injected) --------------------------------\n# Guards every Steam update: a stuck/partial SteamCMD run (state 0x6, \"Missing\n# file permissions\", ENOSPC, transient failure) must NOT be reported as success\n# \u2014 that strands the server on a stale build (\"wrong build\" on join). We key off\n# the app's manifest StateFlags: exactly \"4\" == fully installed & current.\nREFX_APPID=\"258550\"\nrefx_steam_manifest() { echo \"/mnt/server/steamapps/appmanifest_${1}.acf\"; }\nrefx_steam_installed() { local m; m=\"$(refx_steam_manifest \"$1\")\"; [ -f \"$m\" ] && grep -Eq '\"StateFlags\"[[:space:]]+\"4\"' \"$m\"; }\nrefx_steam_heal() { # drop a stale/partial manifest so this update re-acquires cleanly\n  local m; m=\"$(refx_steam_manifest \"$1\")\"\n  if [ -f \"$m\" ] && ! refx_steam_installed \"$1\"; then\n    echo \"[refx] steam app $1: a previous install left a stale manifest (not fully-installed) \u2014 clearing so this update re-acquires the build cleanly.\"\n    rm -f \"$m\" 2>/dev/null || true\n  fi\n  rm -rf \"/mnt/server/steamapps/downloading/$1\" \"/mnt/server/steamapps/temp/$1\" 2>/dev/null || true\n}\nrefx_steam_verify() { # HARD-FAIL if the app is not fully installed after the update\n  if refx_steam_installed \"$1\"; then\n    echo \"[refx] steam app $1 is on the current build (SteamCMD confirmed fully installed).\"\n    return 0\n  fi\n  echo \"[refx] ERROR: SteamCMD did not finish updating app $1 \u2014 the game files were NOT\"\n  echo \"[refx] changed, so the server is still on its previous build and clients may see a\"\n  echo \"[refx] 'wrong build' / version-mismatch error on join.\"\n  echo \"[refx][diag] install uid:gid = $(id -u 2>/dev/null):$(id -g 2>/dev/null)\"\n  df -h /mnt/server 2>/dev/null | sed 's/^/[refx][disk] /' || true\n  ls -ln /mnt/server 2>/dev/null | sed 's/^/[refx][diag] /' | head -20 || true\n  # Clear the stale manifest so the NEXT update self-heals (clean re-acquire).\n  rm -f \"$(refx_steam_manifest \"$1\")\" 2>/dev/null || true\n  echo \"[refx] Cleared the stale manifest \u2014 run Update again and it will re-download the\"\n  echo \"[refx] current build. Your world/config under the data dir are untouched.\"\n  exit 1\n}\n# ---------------------------------------------------------------------------\nrefx_steam_heal \"$REFX_APPID\"\n\nmkdir -p /mnt/server/steamcmd\ncd /mnt/server\nsteamcmd +force_install_dir /mnt/server +login anonymous +app_update 258550 validate +quit\nif [ \"${OXIDE}\" = \"true\" ]; then\n  curl -fsSL -o oxide.zip https://umod.org/games/rust/download/develop\n  unzip -o oxide.zip -d /mnt/server && rm -f oxide.zip\nfi\n\n# Write the ReFx Rust launcher. Rust's Bootstrap.Init_Tier0 sanitizes the rcon\n# password out of log strings via String.Replace(pass,\"***\"); an EMPTY password\n# makes Replace(\"\",...) throw \"String cannot be of zero length\" and the server\n# crashes on boot. The launcher generates a strong random rcon password when the\n# customer leaves it blank, then launches RustDedicated.\ncat > /mnt/server/refx-rust.sh <<'RUSTEOF'\n#!/bin/bash\ncd /home/container 2>/dev/null || cd \"$(dirname \"$0\")\"\nRP=\"${RCON_PASS:-}\"\nif [ -z \"$RP\" ]; then\n  RP=\"$(head -c 24 /dev/urandom 2>/dev/null | base64 | tr -dc 'A-Za-z0-9' | head -c 16)\"\n  [ -z \"$RP\" ] && RP=\"refxRcon${RCON_PORT:-28016}\"\n  echo \"[refx] rcon password was empty; generated a random one (set one in the panel to override).\"\nfi\nexec ./RustDedicated -batchmode \\\n  +server.port \"${SERVER_PORT:-28015}\" \\\n  +server.identity rust \\\n  +rcon.port \"${RCON_PORT:-28016}\" \\\n  +rcon.web 1 \\\n  +rcon.password \"$RP\" \\\n  +server.hostname \"${SERVER_NAME:-A ReFx Hosting Rust Server}\" \\\n  +server.level \"${LEVEL:-Procedural Map}\" \\\n  +server.worldsize \"${WORLD_SIZE:-3500}\" \\\n  +server.seed \"${SERVER_SEED:-1337}\" \\\n  +server.maxplayers \"${MAX_PLAYERS:-50}\" \\\n  +server.saveinterval 300\nRUSTEOF\nchmod +x /mnt/server/refx-rust.sh\nrefx_steam_verify \"$REFX_APPID\"\necho 'Rust install complete.'\n"
  },
  "configFiles": [],
  "recCpuCores": 4,
  "recMemoryMb": 8192,
  "recDiskMb": 30720,
  "variables": [
    {
      "envName": "SERVER_NAME",
      "displayName": "Server Hostname",
      "description": "The name shown in the in-game server browser.",
      "type": "STRING",
      "defaultValue": "A ReFx Hosting Rust Server",
      "rules": {
        "required": true,
        "maxLength": 128
      },
      "userEditable": true,
      "userViewable": true,
      "sortOrder": 0
    },
    {
      "envName": "WORLD_SIZE",
      "displayName": "World Size",
      "description": "Procedural map size in meters. 3000-4500 recommended for most servers.",
      "type": "NUMBER",
      "defaultValue": "3500",
      "rules": {
        "required": true,
        "min": 1000,
        "max": 6000
      },
      "userEditable": true,
      "userViewable": true,
      "sortOrder": 1
    },
    {
      "envName": "SERVER_SEED",
      "displayName": "World Seed",
      "description": "Procedural generation seed. Same seed + size produces the same map.",
      "type": "NUMBER",
      "defaultValue": "1337",
      "rules": {
        "required": true,
        "min": 0,
        "max": 2147483647
      },
      "userEditable": true,
      "userViewable": true,
      "sortOrder": 2
    },
    {
      "envName": "MAX_PLAYERS",
      "displayName": "Max Players",
      "description": "Maximum concurrent player slots.",
      "type": "NUMBER",
      "defaultValue": "50",
      "rules": {
        "required": true,
        "min": 1,
        "max": 500
      },
      "userEditable": true,
      "userViewable": true,
      "sortOrder": 3
    },
    {
      "envName": "LEVEL",
      "displayName": "Map / Level",
      "description": "World level to generate.",
      "type": "ENUM",
      "defaultValue": "Procedural Map",
      "rules": {
        "required": true,
        "options": [
          "Procedural Map",
          "Barren",
          "HapisIsland",
          "SavasIsland_koth"
        ]
      },
      "userEditable": true,
      "userViewable": true,
      "sortOrder": 4
    },
    {
      "envName": "RCON_PORT",
      "displayName": "RCON Port",
      "description": "TCP port for the RCON/web-rcon interface.",
      "type": "NUMBER",
      "defaultValue": "28016",
      "rules": {
        "required": true,
        "min": 1024,
        "max": 65535
      },
      "userEditable": false,
      "userViewable": true,
      "sortOrder": 5
    },
    {
      "envName": "RCON_PASS",
      "displayName": "RCON Password",
      "description": "Password protecting the RCON interface.",
      "type": "SECRET",
      "defaultValue": "",
      "rules": {
        "required": true,
        "minLength": 8
      },
      "userEditable": true,
      "userViewable": false,
      "sortOrder": 6
    },
    {
      "envName": "OXIDE",
      "displayName": "Install uMod/Oxide",
      "description": "Install the uMod (Oxide) modding framework on (re)install.",
      "type": "BOOLEAN",
      "defaultValue": "false",
      "rules": {},
      "userEditable": true,
      "userViewable": true,
      "sortOrder": 7
    }
  ],
  "tags": [
    "pvp",
    "open-world",
    "multiplayer"
  ],
  "longDescription": "Survive, build and raid on your own Rust server. Tune wipe schedules, plugins and player counts for your community."
}
